<?php
/**
 * Created by PhpStorm.
 * User: joey
 * Date: 2/29/2016
 * Time: 2:48 PM
 */

namespace NovakSolutions\FrontDesk\Model\{{ getSubNamespace }};
use NovakSolutions\FrontDesk\Model;

/**
 * Class {{ model.modelName }}
 * @package NovakSolutions\FrontDesk\Model\{{ getSubNamespace }}
{% for field in model.fields %} * @property {{ descriptionToPhpDocType(field.type) }} ${{ field.name }}
{% endfor %}
 */
class {{ model.modelName }} extends Model\{{ baseClass }} {
    public static $endPoints = array(
        'select' => array(
            'httpMethod' => 'POST',
            'urlPath' => '{{ model.urlPath }}'
        )
    );

    public static $fields = array(
{% for field in model.fields %}        '{{ field.name }}',
{% endfor %}
    );
}