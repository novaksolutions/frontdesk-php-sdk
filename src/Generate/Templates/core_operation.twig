<?php
/**
 * Created by PhpStorm.
 * User: joey
 * Date: 2/29/2016
 * Time: 2:48 PM
 */

namespace NovakSolutions\FrontDesk\Method\{{ frontOrDesk }};
use NovakSolutions\FrontDesk\Method;

/**
 * Class {{ method.name }}
 * @package NovakSolutions\FrontDesk\Model\{{ frontOrDesk }}
{% for field in method.fields %} * @property {{ descriptionToPhpDocType(field.type) }} ${{ field.name }}
{% endfor %}
 */
class {{ method.modelName }} extends Model\{{ baseClass }} {
    public static $endPoints = array(
        'select' => array(
            'method' => '{{ method.method }}',
            'urlPath' => '{{ method.urlPath }}'
        )
    );

    public static $fields = array(
{% for field in method.fields %}        '{{ field.name }}',
{% endfor %}
    );
}